package ru.chalovai.PR6;

public interface Comparator {
    //  Интерфейс Comparable определяет метод compareTo(), чтобы определить относительный порядок своих
    //  объектов.
    int compareTo(Student ob);
}
